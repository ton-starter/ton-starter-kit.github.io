# Культура кода

### Оглавление

- **Качество кода**: Следуем принципам KISS, DRY и SOLID.
- **Методология FSD**: Используем Nuxt и FSD для структурирования кода.
- **Линтинг и форматирование**: Автоматизируем проверку кода.
- **Завершение задачи**: Обновляем ченджлог, следим за чистотой консоли и удаляем мёртвый код.
- **Ревью кода**: Проводим коллективное обсуждение для улучшения качества кода.

##### 1. **Качество кода**

- Стремимся к **расширяемому** и **поддерживаемому** коду:

  - **DRY**: Избегаем дублирования кода, выносим повторяющуюся логику в отдельные функции или модули. [Шпаргалка по DRY](https://dev.to/accreditly/dry-what-is-it-and-how-to-implement-it-dont-repeat-yourself-59fj).
  - **KISS**: Упрощаем код, чтобы он был понятным и легко поддерживаемым. [Шпаргалка по KISS](https://medium.com/@Masoncoding/understanding-the-kiss-principle-in-software-design-keep-it-simple-stupid-6f5fcd8913f3).
  - **SOLID**: Следуем принципам SOLID для создания гибкой и масштабируемой архитектуры. [SOLID на Vue](https://www.devsurge.ru/posts/principy-solid-s-primerami-na-js-i-vue).

- Избегаем "срезания углов", которые могут привести к техническому долгу. Если приходится делать быстрые хотфиксы, обязательно возвращаемся к ним, когда появляется время, чтобы переписать и протестировать код.

##### 2. **Методология FSD**

- Предлагаем использовать методологию [**FSD (Feature-Sliced Design)**](https://feature-sliced.github.io/documentation/ru/), которая:
  - Помогает упорядочить компоненты согласно _бизнес-задачам_.
  - Контролирует направление данных, чтобы избежать излишней связанности и запутанности кода - в соответствии с SOLID, особенно принципу Single Responsibility.
- Компонентный подход, заложенный в **Nuxt** адаптируется и _дополняется_ методологией **FSD**. В Nuxt уже есть слои `/pages` и `/app`, а папку `components` мы делим на:

  - **widgets**: Самостоятельные блоки (например, Header, Footer).
  - **features**: Функциональные возможности (например, Auth, Search).
  - **entities**: Базовые сущности (например, User, Token).
  - **shared**: Общие компоненты и утилиты (например, Button, Input).

    Весь код не попадающий в более глобальные app и pages должен быть распределён в этих 4 папках, т.е. рядом ничего не создавать, а внутри этих папок уже ветвить по методолгии.

    ![4слоя](https://i.imgur.com/XrBM5Xj.png)

    При сложностях с определением слоя(папки) для нового компонента можно пользоваться вспомогательными вопросами:
    ![Шпаргалка по FSD](https://i.imgur.com/ddIAqqm.png)

Пример структуры для фичи **Auth**:

```
/components
  /features
    /auth
      /ui*      # Компоненты (например: ModalLogin, ModalRegister)
      /model    # Логика (например: useAuthStore)
      /api      # API-запросы (например: authApi)
```

**\*** Условная папка, компоненты можно располагать как обычно

- Настройки Nuxt подобраны так, что эти 4 папки не учитывались при нейминге (например, `components/shared/Title.vue` импортится как `<Title />`). Главное правило: не называть компоненты с прописной буквы.

##### 3. **Линтинг и форматирование**

Для поддержания общего стиля кода был внедрён [eslint](https://eslint.org/) с prettier(https://prettier.io/)

- Линтер автоматизирован, запускается при пуше в репозиторий и проверяет коммит файлы. Первым отрабатывает форматирование prettier, затем правила и фиксы eslint.
- Для проверки и исправления по всему проекту запустите линтер командой `npx eslint --fix .`.
- Предупреждения (warn) не обязательны для пуша, ошибки - блокируют коммит.
- По возможности файлы автоматически приводятся к единым формату и правилам. Если автоматически исправить не удаётся — отображаются лог с файлами и с строки в которых нужно внести изменения. Нужно вручную подправить эти файлы.
- Пример процесса:
  1. Создаём компонент. Прокидываем **props**-ы в компонент и используем их только в шаблоне. На всякий случай присваиваем их переменной, что бы воспользоваться ею в секции `<script>`:
  2. Закончили работу над фичей, пушим, но eslint выдаёт ошибку `vue/no-unused-vars` и ссылку на файл в котором подчёркнута переменная `props`:

![doc_eslint_1](https://i.imgur.com/qAHCYsY.jpeg)

1. Правило подсказывает, что если по итогу переменная **props** не пригодилась, её можно удалить, извлечённая переменная text остаётся доступной в шаблоне, можно пушить:

![doc_eslint_2](https://i.imgur.com/BdPnzSm.jpeg)

- В ситуации, когда нам очень нужно объявить явно переменную, но не использовать или в асинхронной операции нам линтер обозначает, что мы не используем так же обязательную к объявлению переменную "error", а она нам по факту не нужна в логике, правило настроено так, что можно обойти его символом подчёркивания (underscore):

![doc_eslint_2](https://i.imgur.com/yrGtEQm.jpeg)

- Основные **правила форматирования** prettier встроены в Eslinter

```
 {
    singleQuote: true,
    semi: true,
    tabWidth: 2,
    trailingComma: 'all',
  },
```

- **Правила Eslint** используются стандартные/рекомендуемые. Кастомные правила привидены ниже:

```
{
    rules: {
      // Отключаем правило для максимального количества атрибутов на строку
      'vue/max-attributes-per-line': 'off',

      // Отключаем правило no-undef
      'no-undef': 'off',

      // Отключаем правило @typescript-eslint/no-explicit-any
      '@typescript-eslint/no-explicit-any': 'off',

      // Отключаем правило "multi-word-component-names"
      'vue/multi-word-component-names': 'off',

      // Отключаем правило "vue/no-reserved-component-names"
      'vue/no-reserved-component-names': 'off',

      // Отключаем правило "no-v-html"
      'vue/no-v-html': 'off',

      // Правила для console и debugger
      'no-console': 'warn',
      'no-debugger': 'warn',

      // Правило для отступов в HTML
      'vue/html-indent': [
        'warn',
        2,
        {
          attribute: 1,
          baseIndent: 1,
          alignAttributesVertically: true,
        },
      ],

      'vue/singleline-html-element-content-newline': 'off', // Отключаем требование новой строки для однострочных элементов
      'vue/multiline-html-element-content-newline': 'off', // Отключаем требование новой строки для многострочных элементов
      'vue/html-closing-bracket-newline': 'off', // Отключаем правило новой строки перед закрывающей скобкой
      'vue/attributes-order': [
        'error',
        {
          order: [
            'DEFINITION',
            'LIST_RENDERING',
            'CONDITIONALS',
            'RENDER_MODIFIERS',
            'GLOBAL',
            'OTHER_ATTR',
            'EVENTS',
            'CONTENT',
          ],
          alphabetical: false,
        },
      ],

      // Правило для неиспользуемых переменных
      'vue/no-unused-vars': [
        'error',
        {
          ignorePattern: '^_',
        },
      ],

      '@typescript-eslint/no-unused-vars': [
        'error',
        {
          argsIgnorePattern: '^_',
          varsIgnorePattern: '^_',
          caughtErrorsIgnorePattern: '^_',
        },
      ],

      // Настройки Prettier
      'prettier/prettier': [
        'error',
        {
          singleQuote: true,
          semi: true,
          tabWidth: 2,
          trailingComma: 'all',
        },
      ],
    },
  }
```

##### 4. **Процесс завершения задачи**

При пуше в продакшен:

- **Ченджлог**: Обязательно обновляйте файл `CHANGELOG.md` с описанием изменений. Это помогает отслеживать историю изменений и понимать, что было сделано в каждой версии.
- **Белая консоль**: Убедитесь, что в консоли нет ошибок или предупреждений. Это важно для стабильности приложения.
- **Удаление "мёртвого" кода**: Удаляйте старый функционал, неиспользуемые функции, переменные и импорты. Это упрощает поддержку и уменьшает вероятность ошибок.

##### 5. **Ревью кода**

- После слияния на **stage** по регламенту **коллективное обсуждение кода**, чтобы:
  - Обеспечить соответствие общему код-стайлу.
  - Убедиться в отсутствии мёртвого кода.
  - Проверить чистоту консоли.
  - Обменяться опытом и улучшить качество кода. 💪

---
