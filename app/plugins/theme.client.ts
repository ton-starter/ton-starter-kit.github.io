export default defineNuxtPlugin((_nuxtApp) => {
  // const { initTheme } = useThemeProvider();
  // nuxtApp.provide('theme', useThemeProvider());
  // nuxtApp.hook('app:mounted', () => {
  //   initTheme();
  // });
});
